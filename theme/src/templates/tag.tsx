import React, {FunctionComponent} from "react";
import Layout from "../components/layout";

const TagTemplate: FunctionComponent<{}> = () => (
  <Layout>
    tag
  </Layout>
);

export default TagTemplate;
